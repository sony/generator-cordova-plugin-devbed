language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-cordova-plugin-devbed' ]; then cd .. && eval "mv $currentfolder generator-cordova-plugin-devbed" && cd generator-cordova-plugin-devbed; fi
